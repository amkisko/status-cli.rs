# Gentoo ebuild

Template: status-cli-0.1.0.ebuild

Copy into a local overlay at `app-misc/status-cli/`. For a full offline build, generate a crate-complete ebuild with `cargo ebuild`.

Or install from source:

```sh
cargo install --path status
```

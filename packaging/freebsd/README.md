# FreeBSD port template

Use this Makefile as a starting point for a FreeBSD Ports tree entry under `sysutils/status-cli`, or as a local port.

```sh
make cargo-crates
# then build/install via the Ports workflow
```

Alternatively, with Rust installed:

```sh
cargo install --path status
```

[package]
name = "status"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Status page CLI — search catalog and check live status"

[lib]
name = "status"
path = "src/lib.rs"

[[bin]]
name = "status"
path = "src/main.rs"

[dependencies]
status_lib = { path = "../status_lib" }
clap = { version = "4", features = ["derive", "env"] }
clap_complete = "4"
clap_mangen = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
ratatui = { version = "0.30", default-features = false, features = ["crossterm", "std", "underline-color", "layout-cache"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"

[lints]
workspace = true

# Optional environment configuration for status-cli.
# Copy values into your shell profile or export them as needed.
# No secrets are required for catalog search or public status checks.

# STATUS_OUTPUT=plain
# STATUS_TIMEOUT=10
# STATUS_DEBUG=1
# STATUS_NO_COLOR=1

# Local automation (flags, not env):
#   status check --from ~/.config/status/watchlist.toml --fail-if-degraded --append-jsonl checks.jsonl

# status watch TUI

## Participants

- amkisko

## Decisions

- Add `status watch` as an interactive ratatui dashboard over the same target/--from resolution as `check`.
- Refresh one target per UI tick so the table updates progressively; default interval 30s.
- Keep packaging/binary size impact modest by enabling only crossterm-backed ratatui features.

## Effects

- New modules under `status/src/watch/`.
- README, CHANGELOG, and watchlist example document the command and keys.
- CLI tests cover usage errors and help text for `watch`.

## Next

- Optional: GitHub Release / version bump and Homebrew tap bump after tagging.
- Optional: non-blocking fetch thread if large watchlists make HTTP pauses feel sticky.

## Source

- Conversation request to implement interactive watch after confirming ratatui cost is acceptable.
